export type PlayerStyle = {
  baseFight: string;
  etc: string[];
};

export type PlayerGrade = {
  ranking: number;
};

export type PlayerStat = {
  attack: number;
  defence: number;
  hp: number;
};
